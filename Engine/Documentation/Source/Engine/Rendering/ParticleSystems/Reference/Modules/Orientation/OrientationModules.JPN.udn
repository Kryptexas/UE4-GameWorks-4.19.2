INTSourceChangelist:2074246
Availability:Public
Title:Orientation モジュール
Crumbs: %ROOT%, Engine, Engine/Rendering/ParticleSystems, Engine/Rendering/ParticleSystems/Reference
Description:パーティクル配置を制御するモジュールです。特定の軸に対し回転を防ぎます。

[TOC(start:2 end:3)]

## Orientation モジュール

パーティクル配置を制御するモジュールです。特定の軸に対する回転を防ぎます。

### Axis Lock

Axis Lock モジュールは、指定軸に面しているパーティクルのロックに使用します。現状では、スプライトベースのモジュールのみです。以下のメンバーが含まれます 

![](LockAxis.png)

この画像では、パーティクルスプライトは正の Y 軸にロックされています。

| プロパティ | 説明 |
| ---- | ---- |
|[REGION:tablesection]オリエンテーション[/REGION]||
|**Lock Axis Flags** | パーティクルをロックする軸を示します。以下の値のどれかになります。[INCLUDE:#LockAxisFlags] |






<!-------------Axis Lock Includes----------------
#### Includes


[EXCERPT:LockAxisFlags]
[REGION:simpletable]
|**フラグ**|**説明**|
| --- | --- |
|_EPAL_NONE_|軸に対してロックしません。|
|_EPAL_X_|X軸の正の方向に向かってスプライトをロックします。|
|_EPAL_Y_|Y軸の正の方向に向かってスプライトをロックします。|
|_EPAL_Z_|Z軸の正の方向に向かってスプライトをロックします。|
|_EPAL_NEGATIVE_X_|X軸の負の方向に向かってスプライトをロックします。|
|_EPAL_NEGATIVE_Y_|Y軸の負の方向に向かってスプライトをロックします。|
|_EPAL_NEGATIVE_Z_|Z軸の負の方向に向かってスプライトをロックします。|
|_EPAL_ROTATE_X_|X軸にスプライトの回転をロックします。|
|_EPAL_ROTATE_Y_|Y軸にスプライトの回転をロックします。|
|_EPAL_ROTATE_Z_|Z軸にスプライトの回転をロックします。|
[/REGION]
[/EXCERPT:LockAxisFlags]


------------Axis Lock Includes--------------->