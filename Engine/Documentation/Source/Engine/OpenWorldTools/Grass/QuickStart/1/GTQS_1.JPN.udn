INTSourceChangelist:2576224
Availability:Public
Title:1. 初期レベル設定
Crumbs:
Description:Grass ツールの作業に必要な初期アクタの設定方法
Navigation:topic
SkillLevel:Intermediate
Version:4.8

[VAR:Steps]
[OBJECT:Navigation]
	[PARAM:previous]
	[/PARAM]
	[PARAM:current]
	[/PARAM]
	[PARAM:home]
		[操作手順のホームページ](Engine\OpenWorldTools\Grass\QuickStart "Engine\OpenWorldTools\Grass\QuickStart:Title%")
	[/PARAM]
	[PARAM:next]
	[次の手順](Engine\OpenWorldTools\Grass\QuickStart\2 "%Engine\OpenWorldTools\Grass\QuickStart\2:title%") 
	[/PARAM]
[/OBJECT]
[/VAR]

%Steps%

次のセクションで、 Grasss ツールと一緒に使用する新規レベルとランドスケープテレインを作成します。

1. **Default Template** をベースとして使用する新規レベルを作成します。

	![](Engine\OpenWorldTools\ProceduralFoliage\QuickStart\1\T_New_Level.png)

1. まず **Landscape ツール** をクリックして新しく Landscape アクタを追加して、次にレベルに Landscape アクタを追加するために **Create(作成）** ボタンをクリックします。

	![](Engine\OpenWorldTools\ProceduralFoliage\QuickStart\1\T_Create_Landscape.gif)(convert:false)

1. Grass ツールよりわかりやすく紹介するために、ランドスケープテレインが完全にフラットにならないようにノイズを加えます。
	**Landscape ツール** へ移動して、 **Landscape Editor** セクションで利用可能なツールを表示するために **Sculpt Tool** アイコンをクリックして、リストから **Noise** ツールを選びます。 
	以下に示す値でプロパティを設定しましょう。
	
	![](T_Sculpt_Tool_Settings.png) 

	 |プロパティ名 | 値 | 理由 |
	|--------------|-------|---------|
	|**Brush Size**    |65536.0| ランドスケープテレインに同時に影響をもたらすのに十分な大きさのブラシを作成します。|
	|**Tool Strength** |0.01   | わずかなエフェクトを作成するために、 Tool Strength をとても低い値に設定してペイントすることで明暗度を追加します。|
	|**Noise Scale**   |256    | Noise Scale を大きい値に設定すればするほどノイズがより滑らかになり、ランドスケープに適用するとよりナチュラルな見た目になります。|

1. ランドスケープテレイン 全体を覆うようにランドスケープブラシをビューポートを配置してから、左マウスボタンを **3** 、 **4** 回押して、非常に微細なノイズをランドスケープサーフェスに追加します。 
	操作が完了したら、以下の画像に似た結果となります。

	![](T_Noise_On_Landscape.png)

	[region:note]
	上記で処理したしたランドスケープの修正は、 Grass ツールがどのように動作するかを見せるためのアーティスト的な理由のみで微調整を行いました。必要に応じて、 Grass ツールを完全にフラットな状態のランドスケープテレインに使用することができます。
	[/region]

1. **モード** パネルの **Place** アイコンをクリックしてランドスケープツールを終了します。

	![](T_Exit_LS_Tools.png)





%Steps%