INTSourceChangelist:2407811
Availability:Public
Title:Blend ノード
Crumbs: %ROOT%, Engine, Engine/Animation, Engine/Animation/NodeReference
Description:複数のアニメーションを基準一式に基づき、まとめてブレンドするアニメーション ノード。

[REGION:banner]
![Blend Nodes](blend_banner.png)(convert:false)
[/REGION]

[TOC(start:2)]

[EXCERPT:Intro]
Blend ノードは複数のアニメーションをまとめてブレンドするために使用します。Blend ノードは、 _AnimationBlueprint_ の **Anim Graph** に配置する場合にのみ利用できます。各 Blend ノードは複数のポーズ、最終アウトプットでの各ポーズの重み付けを計算するために使用するアルファ値やウェイト値を取り込みます。Blend ノードの中には、他よりも複雑で、入力に追加のデータを必要とするものもあります。
[/EXCERPT:Intro]

## Apply Additive

**Apply Additive** ノードは、アルファ値に基づき、ベースの通常のアニメーション ポーズに加算ポーズを追加します。

![](ApplyAdditive.png)

| ピン | 説明 |
| --- | ----------- |
| [REGION:tablesection]**入力ピン**[/REGION] ||
| Base | ベースとして使用する通常のポーズ。 |
|Additive| 上にブレンドする加算ポーズ。 |
| Alpha | 加算ポーズがどれくらい影響を及ぼすかを設定するためにアルファ値として使用する**[0.0, 1.0]** の範囲の `浮動小数点値` です。**0.0** という値は、**加算** ポーズが **ベース** 入力に全く加算されないことを表します。**1.0** という値は、**加算** ポーズが **ベース** 入力ポーズに完全に加算されることを表します。
| [REGION:tablesection]**出力ピン**[/REGION] ||
| Pose | ブレンディング適用後の最終ポーズ |

## Blend

標準の **Blend** ノードはアルファ入力値に基づいた 2 つの入力ポーズを正しく混ぜ合わせます。

![](Blend.png)

| ピン | 説明 |
| --- | ----------- |
| [REGION:tablesection]**入力ピン**[/REGION] ||
| A | ブレンドする最初のポーズ |
| B | ブレンドする 2 番目のポーズ |
| Alpha | 2 つのポーズの重み付けを設定する際にアルファ値として使用する**[0.0, 1.0]** の範囲の「浮動小数点」値です値 **0.0** は、**A** の入力ポーズに対して最大限の重み付けをしますが、値 **1.0** は、 **B** の入力ポーズに対して最大限の重み付けをします。|
| [REGION:tablesection]**出力ピン**[/REGION] ||
| Pose | ブレンディング適用後の最終ポーズ |


## Blend Poses by Bool

**Blend Poses by Bool** ノードは、Boolean 値をキーとして使用し、 2 つのポーズ間の時間ベースのブレンドを行うノードです。Boolean が _true_ の場合、 _true_ 入力ピンに接続されているポーズが使用されます。Boolean が _false_ の場合、_false_ のポーズが使用されます。各ポーズには、浮動小数点値、 Blend Time があります。これは、ポーズにブレンドする所要時間を制御するために使用します。

![](BlendPosesByBool.png)

| ピン | 説明 |
| --- | ----------- |
| [REGION:tablesection]**入力ピン**[/REGION] ||
| Active Value | これは、入力 Boolean であり、 2 つのポーズのうちどちらを使用するかを決めるものです。|
| True Pose | Boolean が _true_ に設定されている場合にブレンドするポーズを取り込みます。 |
| False Pose | Boolean が _false_ に設定されている場合にブレンドするポーズを取り込みます。 |
| True Blend Time | _true_ ポーズにブレンドするための所要時間を定義します。 |
| True Blend Time | _false_ ポーズにブレンドするための所要時間を定義します。 |
| [REGION:tablesection]**出力ピン**[/REGION] ||
| Pose | ブレンディング適用後の最終ポーズ |

## Blend Poses by Int

**Blend Poses by Int** ノードは、整数値をキーとして使用し、 任意の数のポーズを時間ベースでブレンドするノードです。各入力整数値に対して、その値の入力ピンに関連付けられたポーズが使用されます。例えば、整数が 0 に設定されている場合、_Blend Pose 0_ に接続されているポーズが使用されます。各ポーズには、浮動小数点値、 Blend Time があります。これは、ポーズにブレンドする所要時間を制御するために使用します。

![](BlendPosesByInt.png)

追加で Blend Pose 入力を作成するには、対象ノード上で **右クリック** し、コンテキスト メニューから**[Add Blend Pin]** を選択します。

![](AddBlendInput.png)

入力を削除するには、削除対象の入力上で **右クリック** して**[Remove Blend Pin]**を選択します。


| ピン | 説明 |
| --- | ----------- |
| [REGION:tablesection]**入力ピン**[/REGION] ||
| Active Child Index | これは、入力 int であり、どのポーズを使用するかを決めるものです。|
|Blend Pose X | Active Child Index が X に設定されている場合にブレンドするポーズを取り込みます。 |
| Blend Time X | X ポーズにブレンドするための所要時間を定義します。 |
| [REGION:tablesection]**出力ピン**[/REGION] ||
| Pose | ブレンディング適用後の最終ポーズ |



## Layered Blend Per Bone

**Layered Blend Per Bone** ノードはスケルトンでボーンの特定のサブセットにのみ影響を及ぼす masked blend を行います。例えば、キャラクタの腰より上にだけアニメーションを適用したい場合は、**Blend Per Bone** を使用して行うことができます。

![Blend Nodes - Blend Per Bone](CE_nodes_blend_per_bone.png)

| ピン | 説明 |
| --- | ----------- |
| [REGION:tablesection]**入力ピン**[/REGION] ||
| BasePose | ブレンドする最初のポーズ |
| BlendPose | ブレンドする 2 番目のポーズ |
| BlendWeight | 加算ポーズがどれくらい影響を及ぼすかを設定するためにアルファ値として使用する **[0.0, 1.0]** の範囲の「浮動小数点」値です。**0.0** という値は、**加算** ポーズが **ベース** 入力に全く加算されないことを表します。**1.0** という値は、**加算** ポーズが **ベース** 入力ポーズに完全に加算されることを表します。
| Add Pin | もうひとつブレンド ポーズとベース入力ポーズを作成します。 |
| [REGION:tablesection]**出力ピン**[/REGION] ||
| OutPose | ブレンディング適用後の最終ポーズ |



## Slot

![](Slotnode.png)

**Slot** ノードは、任意のアニメーション スロットから結果として得られるアニメーションを再生します。 

[INCLUDE:Engine/Animation/AnimMontage#slots]

アニメーション モンタージュ システムでスロットを使用する詳細情報については、以下を参照してください。[](Engine/Animation/AnimMontage)













<!-- Hidden for now.Seems no longer available.

## Two-Way Additive Blend

**Two-way Additive Blend** は実質上、上記の **Two-way Blend** と全く同じです。加算ポーズ上で機能し、加算ポーズを出力する点だけが異なります。

![Blend Nodes - Two-way Additive Blend](CE_nodes_blend_twoway_additive.png)

| ピン | 説明 |
| --- | ----------- |
| [REGION:tablesection]**入力ピン**[/REGION] ||
| A | ブレンドする最初の加算ポーズ |
| B | ブレンドする 2 番目の加算ポーズ |
| Alpha | 2 つのポーズの重み付けを設定する際にアルファ値として使用する**[0.0, 1.0]** の範囲の「浮動小数点」値です値 **0.0** は、**A** の入力ポーズに対して最大限の重み付けをしますが、値 **1.0** は、 **B** の入力ポーズに対して最大限の重み付けをします。|
| [REGION:tablesection]**出力ピン**[/REGION] ||
| ポーズ | ブレンディング適用後の最終加算ポーズ |
--->
